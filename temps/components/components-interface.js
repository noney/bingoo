import state from './com-name-state';
import getters from './com-name-getters';
import actions from './com-name-actions';
import * as comname from './com-name-mutation-types';

/**
 * [mutations description]
 * @type {Object}
 *  example : [comname.SET_EXAMPLE] (state, data) { state.example = data; }
 */
const mutations = {

}

export default {
    state,
    mutations,
    getters,
    actions
}